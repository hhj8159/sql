SELECT 1+1
FROM DUAL 
WHERE 'SQL'='SQL    ';

CREATE TABLE EX_TYPE (
	C CHAR(7),
	V VARCHAR(100),
	N NUMBER(5,2)
);

DROP TABLE EX_TYPE;

SELECT *
FROM EX_TYPE;

INSERT INTO EX_TYPE VALUES('SQL', 'SQL',100.11111);

DELETE FROM EX_TYPE;

SELECT *
FROM EX_TYPE
WHERE C=V || '    ';

SELECT 3.14 + 1 FROM DUAL;

SELECT ROWNUM, STUDENT.*, ROWID FROM STUDENT

WHERE ROWNUM <= 5;
--WHERE GRADE = 2;

-- 현재시간정보회
SELECT SYSDATE FROM DUAL;

SELECT * FROM DEPARTMENT d ;